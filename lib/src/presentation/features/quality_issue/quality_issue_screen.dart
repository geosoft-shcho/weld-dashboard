import '../../core/widgets/placeholder_page.dart';

class QualityIssueScreen extends PlaceholderPage {
  const QualityIssueScreen({super.key})
    : super(
        title: '품질 이슈 연계',
        body:
            '0단계 placeholder. PaperScanHost(pdfrx)는 6단계에서 붙인다. 기동 시 pdfrx는 이미 초기화한다.',
      );
}
